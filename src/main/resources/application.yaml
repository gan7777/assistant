spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/assistant
    username: admin
    password: 123456
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update       # 自动建表，非常方便
    show-sql: true           # 打印SQL日志，方便调试
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081                 # 项目启动后的访问端口

jwt:
  secret: gan7777-ai-assistant-secret-key-2026-very-long-and-secure
  expiration: 86400000  # 24小时，单位毫秒

ai:
  qwen:
    api-key: sk-bdf64f94c60b42038413ebed47d5bedf
    model: qwen-plus